## open-inspector config

Inspect a Terraform/OpenTofu module directory

### Synopsis

config parses every .tf, .tf.json, .tofu, and .tofu.json file
in <dir> (non-recursive) and prints either a human-readable summary
table or a versioned JSON envelope.

```
open-inspector config <dir> [flags]
```

### Options

```
      --fail-on string   exit nonzero if a diagnostic with this severity is present: error|warning|never (default "error")
  -h, --help             help for config
      --json             emit machine-readable JSON instead of a human table
      --schema string    enrich resources with findings from a tofu/terraform 'providers schema -json' document; pass a file path or 'auto' to shell out
```

### Options inherited from parent commands

```
      --log-level string   log-level: debug|info|warn|error (default "info")
      --no-color           disable colored output
      --quiet              suppress informational log output
```

### SEE ALSO

* [open-inspector](open-inspector.md)	 - Inspect Terraform/OpenTofu configurations.

###### Auto generated by spf13/cobra on 9-Jun-2026
